name: base
description: A new brick created with the Mason CLI.

# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
# repository: https://github.com/my_org/my_repo

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0+1

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ^0.1.0
vars:
  name:
    type: string
    description: |
      نام پروژه Flutter شما که در فایل `pubspec.yaml` تعریف شده است. این نام در بسیاری از قسمت‌های پروژه، مانند شناسه پروژه، نام بسته (package) و موارد دیگر استفاده می‌شود. به طور پیش‌فرض، این متغیر به عنوان نام پروژه‌ای با مقدار `my_template` تنظیم شده است.
    default: my_template
    prompt: What is your project name?

